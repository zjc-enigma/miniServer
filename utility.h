#ifndef UTILITY_H
#define UTILITY_H

#define min(a,b)   ((a) < (b) ? (a) : (b))
#define max(a,b)   ((a) > (b) ? (a) : (b))

#define FALSE 0
#define TRUE 1

//typedef unsigned char bool;

#endif //UTILITY_H
